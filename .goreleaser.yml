builds:
  - binary: osmcoverer
    goos:
      - windows
      - darwin
      - linux
    goarch:
      - amd64
archive:
  format: tar.gz
  replacements:
    amd64: 64-bit
    darwin: macOS
  format_overrides:
    - goos: windows
      format: zip
  files:
    - Readme.txt
